# Work Log - 2026-03-07 04:51 JST - #04

## 작업 요약
- WebSocket 이벤트에 `request_id` 멱등 처리 로직을 추가함.
  - 같은 `request_id` 재수신 시 이전 응답 재전송
  - 중복 처리로 상태 변경이 다시 일어나지 않도록 보장
- Redis 기반 테이블 스냅샷 저장/복구 기능을 추가함.
  - join/leave/disconnect 시 스냅샷 저장
  - 서버 시작 시 기본 테이블 스냅샷 복구
- 테이블 스냅샷 복구를 위한 `table.Restore`/`manager.Restore` 기능을 추가함.
- 프론트에서 WS 이벤트 전송 시 `request_id`를 포함하도록 수정함.

## 테스트/검증
- `go test ./...` 통과
- `pnpm build` 통과
- 신규 테스트:
  - WS 멱등 join 테스트
  - 스냅샷 복구 테스트

## 주요 변경 파일
- `backend/internal/app/app.go`
- `backend/internal/app/ws_hub.go`
- `backend/internal/store/table_snapshot_store.go`
- `backend/internal/table/table.go`
- `backend/internal/table/manager.go`
- `backend/internal/protocol/ws.go`
- `web/src/App.tsx`
