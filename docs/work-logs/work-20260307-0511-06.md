# Work Log - 2026-03-07 05:11 JST - #06

## 작업 요약
- PostgreSQL 영속화 계층(EventStore)을 추가함.
  - 게스트 세션 생성 시 `users`, `sessions` 저장
  - 테이블 상태 변경(join/leave/disconnect) 시 `table_events` 저장
- 스키마 마이그레이션 러너를 추가하고 앱 시작 시 자동 적용되도록 연결함.
- Redis 스냅샷 저장과 Postgres 이벤트 저장을 앱 초기화/도메인 흐름에 함께 연결함.

## 추가/수정 파일
- `backend/internal/store/event_store.go`
- `backend/internal/store/postgres_event_store.go`
- `backend/internal/store/migrations/0001_init.sql`
- `backend/internal/app/app.go`
- `backend/internal/config/config.go`

## 검증
- `go test ./...` 통과
- 런타임 점검:
  - `POST /v1/session/guest` 201 확인
  - Postgres 조회 결과 `users=1, sessions=1, table_events=0` 확인
