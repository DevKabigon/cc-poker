# Work Log

- 시간(JST): 2026-03-07 15:08
- 작업 요약:
  - `pnpm dlx shadcn@latest init`를 `web` 경로에서 Tailwind v4 기준으로 정상 완료.
  - `vite.config.ts`에 `@tailwindcss/vite` 플러그인 추가.
  - ESM 환경에서 `__dirname` 경고가 발생하지 않도록 `fileURLToPath(new URL(..., import.meta.url))` 기반 alias로 수정.
  - TS alias 인식 보완(`tsconfig.app.json`) 및 Node 타입 보완(`tsconfig.node.json`, `@types/node`).
  - CSS import 순서 경고 제거를 위해 폰트 import 위치 정리.
- 검증:
  - `pnpm build` 성공 (TypeScript + Vite build 통과).
