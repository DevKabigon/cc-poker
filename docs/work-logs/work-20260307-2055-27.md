# Work Log

- 시간(JST): 2026-03-07 20:55
- 작업 요약:
  - 새로고침 시 세션이 사라지는 문제를 해결하기 위해 백엔드에 `GET /v1/session/current` 엔드포인트를 추가.
  - 프론트 `usePlayConsole`에 세션 부트스트랩(SWR) 로직을 추가해 쿠키 기반 세션을 자동 복구.
  - 로그인/게스트 시작/로그아웃 시 SWR 캐시(`/v1/session/current`, `/v1/wallet`)를 함께 동기화하도록 보강.
  - 현재 세션 조회 API 테스트 2건을 추가:
    - `TestCurrentSessionReturnsSessionFromCookie`
    - `TestCurrentSessionRejectsWithoutCookie`
- 검증:
  - `go test ./...` (backend) 성공
  - `pnpm -C web build` 성공
