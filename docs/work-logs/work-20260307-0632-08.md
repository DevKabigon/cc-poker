# Work Log - 2026-03-07 06:32 JST

## 요약
- 게스트/인증 유저 지갑 정책을 분리했다.
  - 게스트 초기 지갑: 2,000
  - 로그인 + 이메일 인증 유저 초기 지갑: 10,000
- Supabase access token을 백엔드 세션 쿠키로 교환하는 인증 흐름을 추가했다.
  - `POST /v1/auth/exchange`
  - `POST /v1/auth/logout`
- 기존 바이인 선행 흐름과 결합되도록 테스트를 확장했다.
- 프론트에 라우팅/전역 상태/데이터 페칭 계층을 도입했다.
  - `react-router-dom`, `zustand`, `swr`
  - `/play`, `/policy` 라우트 추가
  - Supabase signup/signin + backend exchange 연동

## 백엔드 변경
- `backend/internal/auth/supabase_verifier.go` 추가
  - Supabase `/auth/v1/user` 검증 구현
  - 이메일 미검증 차단 에러 정의
- `backend/internal/app/app.go`
  - auth 교환/로그아웃 라우트 추가
  - 지갑 초기 지급 정책 분리 적용
- `backend/internal/session/store.go`
  - 지정 player_id 세션 생성 메서드 추가
  - 세션 삭제 메서드 추가
- `backend/internal/config/config.go`
  - `CC_POKER_GUEST_WALLET_INITIAL`
  - `CC_POKER_AUTH_WALLET_INITIAL`
  - `CC_POKER_SUPABASE_*` 설정 추가
- 테스트 보강
  - 인증 미검증 거부
  - 인증 세션 발급 성공
  - 게스트 2,000 한도 검증

## 프론트 변경
- `web/src/main.tsx`
  - `BrowserRouter` 적용
- `web/src/store/useAppStore.ts` 추가
  - 세션/에러 전역 상태(zustand)
- `web/src/App.tsx`
  - 라우팅 구조(`/play`, `/policy`)
  - SWR health polling
  - Supabase signup/signin + backend exchange
- `web/src/styles.css`
  - 라우트 네비 링크 스타일 추가
- 환경변수 예시 파일 추가
  - `backend/.env.example`
  - `web/.env.example`

## 검증
- `go test ./...` 통과
- `pnpm -C web build` 통과

