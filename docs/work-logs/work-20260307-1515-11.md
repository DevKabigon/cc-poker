# Work Log

- 시간(JST): 2026-03-07 15:15
- 작업 요약:
  - 프론트에 shadcn UI 컴포넌트를 본격 적용(`Button`, `Card`, `Input`, `Label`, `Badge`, `Alert`).
  - 인증 전용 페이지 `/auth` 추가 및 라우팅 분리.
  - `PlayPage`를 플레이 동작 중심으로 정리하고 인증 안내를 `/auth`로 연결.
  - `PolicyPage`, `TopNav`, `StatusBadge`를 shadcn 기반으로 업데이트.
  - 기존 글로벌 CSS에서 shadcn 스타일과 충돌하는 `button/input/a` 규칙 정리.
- 검증:
  - `pnpm -C web build` 성공.
