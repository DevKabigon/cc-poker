# Work Log

- 시간(JST): 2026-03-07 17:14
- 작업 요약:
  - 백엔드에 `GET /v1/wallet` 엔드포인트를 추가해 쿠키 인증 기준 현재 지갑 잔액 조회를 지원.
  - `EventStore` 인터페이스에 `GetWalletBalance`를 추가하고 noop/postgres 구현을 반영.
  - 지갑 조회 동작 검증을 위한 백엔드 테스트(`TestWalletEndpointReturnsCurrentBalance`)를 추가.
  - 프론트 `usePlayConsole`에 SWR 지갑 캐시(`/v1/wallet`)를 연결하고, 바이인 성공 시 `balance_after` 기반 SWR mutate 적용.
  - 로그아웃 시 지갑 캐시를 즉시 비우고, Auth/Play 페이지 상태 영역에 지갑 잔액 표시를 추가.
  - 세션 스냅샷 타입에 `user_type` 필드를 확장해 지갑 데이터와 연결.
- 검증:
  - `go test ./...` (backend) 성공
  - `go build ./...` (backend) 성공
  - `pnpm -C web build` 성공
  - `pnpm -C web lint`는 `web/package.json`에 lint script가 없어 실행 불가
