# Work Log - 2026-03-07 04:36 JST - #03

## 작업 요약
- `docker-compose.yml`을 추가해 Postgres/Redis 개발 인프라를 구성함.
- WebSocket 브로드캐스트 허브를 구현해 다중 클라이언트가 같은 테이블 스냅샷을 동기화 받도록 수정함.
- 클라이언트 연결 종료 시 자동 퇴장 처리 및 스냅샷 브로드캐스트를 추가함.
- React + Vite 최소 클라이언트를 `web` 폴더에 구성함.
  - 게스트 세션 발급
  - WS 연결
  - `join_table` / `leave_table` 전송
  - 테이블 스냅샷/이벤트 시각화

## 검증
- 백엔드: `go test ./...`, `go build ./...` 통과
- 프론트: `pnpm install`, `pnpm build` 통과
- 도커: `docker compose up -d postgres redis` 실행 후 컨테이너 healthy 확인

## 주요 변경 파일
- `docker-compose.yml`
- `backend/internal/app/app.go`
- `backend/internal/app/ws_hub.go`
- `backend/internal/app/app_test.go`
- `web/*` (Vite React 앱 전체)
