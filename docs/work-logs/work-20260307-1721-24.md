# Work Log

- 시간(JST): 2026-03-07 17:21
- 작업 요약:
  - 로그인 후 지갑이 `$0`으로 보인다는 이슈를 진단했다.
  - Postgres 실데이터 확인 결과(auth 유저 `usr_...`) 지갑 잔액은 `10000`으로 정상임을 확인했다.
  - 프론트에서 `/v1/wallet` 실패 시 `0`으로 폴백 표시되는 UX 문제를 수정했다.
  - `usePlayConsole`에 `walletError`를 노출하고 Auth/Play 페이지에서 실패 시 `unavailable` + 에러 Alert를 표시하도록 변경했다.
- 검증:
  - `pnpm -C web build` 성공
