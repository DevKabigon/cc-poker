# Work Log

- 시간(JST): 2026-03-08 00:40
- 작업 요약:
  - `backend/internal/app/app.go`를 동작 변경 없이 역할별 파일로 분리했다.
    - `app_routes.go`: 라우트 등록
    - `app_types.go`: 요청/응답 DTO
    - `http_handlers.go`: HTTP 핸들러
    - `ws_handlers.go`: WS 핸들러/브로드캐스트
    - `state_helpers.go`: 스냅샷/영속화/환급 보조 로직
    - `session_helpers.go`: 세션 쿠키/Origin/테이블 ID 보조
    - `error_mapping.go`: 에러 코드/HTTP 매핑
    - `auth_helpers.go`: auth 식별자/닉네임 보조 함수
    - `json_helpers.go`: JSON 응답 공통 함수
  - 테이블 페이지에 실제 포커 테이블 형태 UI를 추가했다.
    - `PokerTableView` 컴포넌트 신규 추가
    - 중앙 커뮤니티 카드 영역 + 9석 배치 + 플레이어 카드 표시
    - 테이블 페이지에서 기존 단순 좌석 그리드 대신 신규 UI 사용
  - 관련 스타일을 `styles.css`에 추가했다.
- 검증:
  - `go test ./...` (backend) 성공
  - `pnpm -C web build` 성공
