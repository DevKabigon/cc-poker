# Work Log

- 시간(JST): 2026-03-07 17:31
- 작업 요약:
  - `air` 실행 시 출력되던 경고(`build.bin is deprecated`)를 제거하기 위해 `backend/.air.toml`을 최신 방식으로 정리.
  - `[build]`에서 `bin` 키를 제거하고 `entrypoint` 기반 실행 설정으로 변경.
- 검증:
  - 설정 파일 변경 후 `air` 재실행 시 경고 메시지 소거 여부는 사용자 실행 환경에서 즉시 확인 가능
