# Work Log

- 시간(JST): 2026-03-07 21:17
- 작업 요약:
  - 프론트 라우팅을 `로비 -> 룸 -> 테이블` 구조로 분리.
    - `/lobby`
    - `/rooms/:roomId`
    - `/tables/:tableId`
  - 룸/테이블 카탈로그 모델(`roomCatalog`)을 추가해 룸별 10개 테이블 선택 흐름을 구성.
  - 내비게이션을 `Auth/Lobby/Wallet Policy` 기준으로 갱신하고, `/rooms/*`, `/tables/*` 경로에서도 Lobby 탭이 활성화되도록 개선.
  - Auth 페이지의 진입 버튼을 `/lobby`로 변경.
  - 신규 페이지 UI에 맞춰 카드 그리드 스타일(`room-grid`, `table-select-grid`)을 추가.
- 검증:
  - `pnpm -C web build` 성공
