# Work Log

- 시간(JST): 2026-03-07 15:51
- 작업 요약:
  - `next-themes` 도입 및 앱 루트에 `ThemeProvider` 적용.
  - TopNav에 라이트/다크/시스템 전환 `ThemeToggle` UI 추가.
  - 버튼 가독성 개선을 위해 `Button` variant 대비/hover 스타일 재정의.
  - `styles.css`를 테마 토큰(`var(--...)`) 기반으로 재작성하고 색상 하드코딩 제거.
  - 다크/라이트 모드 대응을 위해 `index.css` 토큰에 `destructive-foreground` 포함 보강.
- 검증:
  - `pnpm -C web build` 성공.
