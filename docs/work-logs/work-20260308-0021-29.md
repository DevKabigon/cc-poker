# Work Log

- 시간(JST): 2026-03-08 00:21
- 작업 요약:
  - `테이블 -> 룸 목록` 이동 시 바이인 환급이 즉시 안 보이던 원인을 확인.
  - 원인: `usePlayConsole` 훅이 unmount될 때 WebSocket 정리를 하지 않아 disconnect 기반 환급 트리거가 즉시 발생하지 않음.
  - 수정: `usePlayConsole`에 unmount cleanup(`closeSocket`)를 추가해 페이지 이탈 시 소켓을 즉시 종료하도록 반영.
  - 효과: 테이블 페이지에서 벗어나는 즉시 서버 `disconnect_leave` 경로가 실행되어 환급 로직이 즉시 동작.
- 검증:
  - `pnpm -C web build` 성공
