# Work Log

- 시간(JST): 2026-03-07 15:22
- 작업 요약:
  - 기존 색상/호버/가독성 문제를 해결하기 위해 UI 테마를 전면 교체.
  - `index.css`에서 shadcn 토큰을 신규 팔레트(라이트 베이스 + 코발트/앰버 포인트)로 재정의.
  - `styles.css`를 재작성해 배경/패널/타이포/레이아웃/모바일 반응형/등장 애니메이션 적용.
  - shadcn `Button`, `Badge`, `Input` 스타일을 프로젝트 테마에 맞게 재구성하고 hover 대비 개선.
  - `Auth`, `Play`, `Policy`, `TopNav`, `StatusBadge` 화면에 새 스타일 시스템 적용.
- 검증:
  - `pnpm -C web build` 성공.
