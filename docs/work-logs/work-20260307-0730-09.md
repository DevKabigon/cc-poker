# Work Log - 2026-03-07 07:30 JST

## 요약
- 닉네임 유니크 정책을 백엔드 전 계층에 반영했다.
  - 런타임 세션 생성 시 닉네임 충돌 즉시 차단
  - Postgres 저장 시 닉네임 unique violation을 도메인 에러로 매핑
  - API에서 충돌 시 `409 Conflict` 반환
- 기존 데이터와의 호환을 위해 닉네임 유니크 인덱스 마이그레이션을 추가했다.
- 인증 fallback 닉네임이 중복되지 않도록 사용자 ID 기반으로 보강했다.
- 테스트를 유니크 정책 기준으로 갱신하고 회귀 테스트를 통과했다.

## 검증
- `go test ./...`
- `pnpm -C web build`

